# Cassini 本地验收记录 · 2026-09-08

## 测试环境与方法

当前分支 `presentation-architecture`；沿用用户未提交的本地架构。系统 macOS 15.2 (24C101)，arm64，Node v24.16.0；Chrome 152.0.7977.83，通过现成 Playwright Core 驱动独立浏览器上下文。未安装依赖、未使用个人浏览器资料、未推送、未部署。

服务地址：`http://localhost:5174`，Vite 开发服务器，无网络或 CPU 限速。视口实际设置为 1440×900、1280×800、390×844，减少动态效果开启；390px 是桌面浏览器视口模拟，不是实体手机。数值和文件比较在 Node 中独立读取 bin、校验 SHA-256、按 Float64 解码，不复用前端导出函数自证。

主验收时间（UTC）：2026-09-09T01:04:12.554Z。最终机器记录见 [主验收 JSON](../artifacts/acceptance/browser-results.json) 与 [补充验收 JSON](../artifacts/acceptance/browser-results-supplemental.json)。

## 已通过

- `npm test`：18/18，包括空白/缺失/负值、方向、断点、闭区间、失败重试、哈希、293,530行大数组导出、六组真实数据、保存、跨标签合并、损坏/失败存储、分享隐私和事件去重。
- `npm run lint` 与 `npm run build` 通过。构建提示按需3D场景包大于500kB；没有隐藏提示。真实浏览器确认首页初次加载不请求该场景包，也不请求完整精确观测数据。
- 现有 `data:verify:catalog` 已实际通过：6组观测、1,566,902条精确转换记录、193个分块哈希、6,114个概览点。源文件抽查为每观测52条确定性对照，加概览重新计算与局部窗口核对；这不等同于宣称每个原始TAB字段都做了全量独立比较。
- 主浏览器验收20/20：首页到真实示例、键盘双点检查、保存与刷新、CSV/Markdown下载、六组观测各三个变量的真实数值与索引、快速切换旧请求、空/反向/越界/空样本区间、边界样本导出、可见导航器拖动与键盘同步、回访和后续任务、改名删除、分享失败后复制框及干净会话不带笔记、存储失败保留和导出、精确/元数据/目录加载失败重试、旧版本和失效链接、前进后退/立即刷新笔记、研究与资源页、三种视口和键盘数据表。
- 桌面1280×800与1440×900的 Apply range 按钮底部均为约792.3px，在首屏内可见。三个视口核心路径没有页面横向溢出。
- 教学 cubic 示例另以1001个参数实际检查，最大方程残差2.84e-14；折点两侧3→1个实根、折点2个不同根，标明 Illustrative model。

补充浏览器验收3/3通过：原生观测选择器正确保留区间交集并解释无交集回退；原值重复应用不激活保存且不可用变量有提示；同观测双标签页的独立草稿与命名保存互不覆盖。合计23个Chrome场景通过。

## 未通过

最终主验收与补充验收均没有未解决的失败。迭代过程中出现过测试脚本的同步等待、精确文本定位和请求拦截隔离问题；已修正并真实复跑。范围拖动测试先将画布滚动到视口内，再执行真实鼠标拖动，没有通过改写业务状态代替交互。

## 尚未运行与保留限制

- WebKit / Safari：现成 Playwright WebKit 可执行文件缺失，没有下载引擎或修改用户Safari自动化设置。已延长下载Object URL存活时间，但不能据此声称Safari下载通过。
- 线上真实用户第75百分位LCP/INP/CLS、回访/留存率及五位目标访客的独立可用性测试：当前没有线上样本，自动测试不能替代。
- 带科学注记的图像导出：未添加；目前可靠交付为精确CSV与观察记录Markdown。
- 正式研究材料仍缺驻相根产物、分支记录、项目诊断、重建结果与已发布稿件/图生成材料。页面保留准确未发布状态，不将DLP或教学cubic称为项目完成的重建。

## 本地性能实测

- 首页入口点击至精确窗口可操作：264 ms。
- 入口至第一次键盘检查：375 ms；包含中间截图与断言开销，不是未受测量干扰的人类任务用时。
- 此次首页 PerformanceObserver：LCP 160 ms，CLS 0.0374。
- 同一SPA文档截至检查/保存所观察到的最大交互事件时长：24 ms。这不是线上INP或第75百分位估计。
- 主要开销被限制为示例单个262,144-byte精确块及对应metadata/index；首页仅使用一个轻量概览。3D说明图由用户点击后才加载。

## 真实截图

1. [首页：用途、主入口与真实概览](../artifacts/acceptance/01_home.png)
2. [已加载的示例窗口](../artifacts/acceptance/02_loaded_example.png)
3. [已选中的精确源样本](../artifacts/acceptance/03_exact_sample.png)
4. [保存后刷新恢复标题、笔记与样本](../artifacts/acceptance/04_saved_restored.png)
5. [390px手机工作台与样本检查](../artifacts/acceptance/05_mobile_workbench.png)

截图均来自实际浏览器。观察文本由验收脚本填入，用于确认保存与隐私行为，不是用户作出的研究结论。没有把失败调试文本当作成功截图。

## 真实导出及数值核对

[完整示例CSV](../artifacts/acceptance/chromium_example_window.csv) 与 [观察记录Markdown](../artifacts/acceptance/chromium_example_observation.md) 均通过页面按钮实际下载，浏览器报告下载完成且无错误。

- Rev133 `RSS_2010_170_X43_E_DLP_500M.TAB`，web数据版本1.1.0。
- 闭区间133625.75–133881.75 km；1,025条记录；源索引247552–248576。
- 5个导出字段（源索引+4个公开变量）与独立解码结果逐值一致；首末边界包含。
- 所有968个负数值原样保留（跨各字段累计计数，不是968个负光学深度点）。此真实窗口缺失值数为0；缺失值保留行为由合成边界测试单独核验，没有伪造真实缺失记录。
- 样本247553：半径133626 km，normal optical depth -0.001269143，normalized signal power 1.039298，phase shift -19.126421 degrees，与截图和CSV一致。
- [两条边界记录CSV](../artifacts/acceptance/chromium_inclusive_two_rows.csv) 也与原记录逐值一致。
- Markdown包含数据身份、版本、问题、自填观察、限制、导出时间和不带笔记的视图链接。链接使用此次本地服务地址，重开需要本地服务运行。

更改文件及实现取舍见 [实施记录](EXPLORATION_IMPLEMENTATION_2026-09-08.md)。
